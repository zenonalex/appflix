import 'package:flutter/material.dart';

mixin AppColors {
  // General
  static const main = Color(0xFF082138);
  static const background = Color(0xFFFFFFFF);
  static const primary = Color(0xFF22CDA4);
  static const shadow = Color(0xFF11ACD8);
  static const lowRate = Color(0xFFf80707);
  static const mediumRate = Color(0xFFe7e938);
  static const higRate = Color(0xFF25c974);

  // Text
  static const primaryText = Color(0xFFFFFFFF);
  static const secondaryText = Color(0xFF1A1A1A);
  static const textPlaceholder = Color(0xFF808586);
}
